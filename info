; (folderName): Групування маршрутів без впливу на URL.

; [folderName]: Динамічні маршрути з одним параметром.

; [...folderName]: Catch-all маршрути (обробляють вкладені URL).

; [[...folderName]]: Опціональні catch-all маршрути (можуть бути порожніми).